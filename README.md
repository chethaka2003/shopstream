# ShopStream E-commerce Analytics Pipeline

## Overview
This project implements the internship case study using:
- MySQL for relational storage
- Python for ETL pipeline
- Cube.js for semantic layer
- React + Recharts for analytics dashboard

## Dataset
Generate the dataset with:

```bash
pip install faker pandas numpy
python generate_dataset.py